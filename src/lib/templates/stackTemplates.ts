// Re-export all templates from the modular files for backward compatibility
// This preserves all existing import paths

export type { StackTemplateCard, StackTemplate } from './basic'
export { stackTemplates } from './index'
